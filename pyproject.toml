[project]
name = "swiftroute-challenge"
version = "1.0.0"
description = "SwiftRoute — Cuộc thi tối ưu giao hàng chặng cuối"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=7.0"]

[tool.setuptools]
packages = ["swiftroute"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
